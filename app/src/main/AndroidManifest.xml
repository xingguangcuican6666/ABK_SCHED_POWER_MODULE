<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.abk.kernel.action.ABK_EXTENSION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="com.abk.kernel.extension.ID"
                android:value="sched_power_profile" />
            <meta-data
                android:name="com.abk.kernel.extension.NAME"
                android:value="ABK Sched Power Extension" />
            <meta-data
                android:name="com.abk.kernel.extension.OOBE_ACTIVITY"
                android:value=".OobeActivity" />
            <meta-data
                android:name="com.abk.kernel.extension.SETTINGS_ACTIVITY"
                android:value=".SettingsActivity" />
        </activity>

        <activity
            android:name=".OobeActivity"
            android:exported="true" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_UNLOCKED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".SchedulerPolicyService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
